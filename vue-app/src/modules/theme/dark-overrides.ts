import type { GlobalThemeOverrides } from "naive-ui";

const theme: GlobalThemeOverrides = {
  common: {
    primaryColor: "#80ff2c",
  },
  Switch: {
    railColor: "#f5f5f5",
    railColorActive: "#80ff2c",
  },
};

export default theme;
